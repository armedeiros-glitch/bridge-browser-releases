name: Deploy Bridge Browser Site

on:
  push:
    branches: [main]
    paths:
      - index.html
      - connect.html
      - privacy.html
      - terms.html
      - support.html
      - bridge-icon.svg
      - .github/workflows/pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Prepare static site
        run: |
          mkdir -p _site
          cp index.html _site/index.html
          cp connect.html _site/connect.html
          cp privacy.html _site/privacy.html
          cp terms.html _site/terms.html
          cp support.html _site/support.html
          cp bridge-icon.svg _site/bridge-icon.svg
          touch _site/.nojekyll
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
